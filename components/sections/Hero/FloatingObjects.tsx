/**
 * Декор отключён — он вызывал рывки при скролле даже как статичный radial-gradient.
 * Hero достаточно «дышит» за счёт background-блока секции.
 */
export function FloatingObjects() {
  return null;
}
